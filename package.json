{
  "name": "agent-communications",
  "version": "0.1.0",
  "private": true,
  "description": "Agent Skills, a CLI and an MCP server that let coding agents work with Gmail safely across many inboxes.",
  "type": "module",
  "license": "MIT",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22.18.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/crissmoldovan/agent-communications.git"
  },
  "scripts": {
    "build": "pnpm -r --filter \"./packages/**\" run build",
    "typecheck": "pnpm -r --filter \"./packages/**\" run typecheck",
    "test": "node --test \"test/**/*.test.mjs\" && pnpm -r --filter \"./packages/**\" run test",
    "lint": "biome ci .",
    "format": "biome format --write .",
    "verify:skills": "node scripts/sync-skills.mjs --check && node scripts/verify-skills.mjs",
    "verify": "pnpm lint && pnpm build && pnpm typecheck && pnpm test && pnpm verify:skills && pnpm verify:versions && pnpm verify:licenses && pnpm verify:packages",
    "release": "node scripts/release.mjs",
    "release:publish": "node scripts/release.mjs --publish",
    "verify:packages": "node scripts/verify-package.mjs packages/core && node scripts/verify-package.mjs packages/gmail && node scripts/verify-package.mjs packages/gmail-mcp",
    "sync:skills": "node scripts/sync-skills.mjs",
    "sync:versions": "node scripts/sync-versions.mjs",
    "verify:versions": "node scripts/sync-versions.mjs --check",
    "licenses": "node scripts/third-party-licenses.mjs",
    "verify:licenses": "node scripts/third-party-licenses.mjs --check"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/node": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  }
}
